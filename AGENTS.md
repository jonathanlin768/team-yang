# 项目规则

## Git 操作

- 对本地代码的修改，**不要主动执行 `git commit` / `git push`**（包括部署用的 `gh-pages` 分支）。
- 只有当用户在对话中**明确提到 git / 提交 / 推送 / 部署**时，才执行相应的提交与推送操作。
- 日常修改完成后，构建验证（`npm run build`）可以照常运行，但改动保持在工作区即可，由用户决定何时提交。
